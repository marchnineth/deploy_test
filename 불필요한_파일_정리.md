# 불필요한 파일 정리 가이드

## 삭제해야 할 파일 및 폴더

### 1. Next.js 관련 폴더 (이 프로젝트는 Vite + React 사용)
- `app/` - Next.js App Router용 폴더
- `components/` - Next.js용 컴포넌트 (실제로는 `src/components/` 사용)
- `lib/` - Next.js용 유틸리티 폴더
- `public/` - Next.js용 public 폴더 (이 프로젝트는 `images/` 폴더 직접 사용)

### 2. 중복 배치 파일
- `cleanup.bat` - 불필요
- `cleanup_unused_files.bat` - 불필요
- `DELETE_UNUSED.bat` - 실행 후 삭제 가능

### 3. 유지할 파일
- `push_to_github.bat` - GitHub push용
- `README.md` - 프로젝트 설명
- `src/` - 실제 프로젝트 소스 코드
- `images/` - 퍼즐 이미지 파일
- `package.json`, `vite.config.js`, `tailwind.config.js` 등 설정 파일

## 삭제 방법

`DELETE_UNUSED.bat` 파일을 더블클릭하여 실행하세요.

또는 수동으로:
1. 파일 탐색기에서 `dinopuzzle` 폴더 열기
2. `app`, `components`, `lib`, `public` 폴더 삭제
3. `cleanup.bat`, `cleanup_unused_files.bat` 파일 삭제
